#include "ParallelCopyTool.hpp"

ParallelCopyTool::ParallelCopyTool(std::unique_ptr<IFileReader> fileReader,
        std::unique_ptr<IFileWriter> fileWriter, std::shared_ptr<IQueue> shQueue)
{
    ;
}

void ParallelCopyTool::copy()
{
    ;
}