#pragma once
// Header files that stores information about buffers used in the tool

#define BUFFER_SIZE 4096 /// Size of one buffer within a queue
#define BUFFERS_IN_QUEUE 512 /// Amount of buffers in the queue
#define TCP_BUFFERS_IN_QUEUE 2048 /// Amount of tpc buffers in the tcp queue